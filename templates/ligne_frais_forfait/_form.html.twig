{{ form_start(form) }}

    <div class="row">

        <div class="col-lg-4 col-md-4 col-sm-12"></div>
        <div class="col-lg-4 col-md-4 col-sm-12">
            <div class="showback">
                <h4 class="bleu-gsb">Informations à renseigner</h4>
                {{ form_row(form.mois, {'label': 'Mois (mois/année)'}) }}
                {{ form_row(form.quantite, {'label': 'Quantité'}) }}
                {{ form_row(form.fkFraisForfait, {'label': 'Forfait'}) }}
                {{ form_row(form.fkVisiteur, {'label': 'Visiteur'}) }}
            </div>
        </div>
        <div class="col-lg-4 col-md-4 col-sm-12"></div>

    </div>

    {{ form_widget(form) }}
    
    <button class="btn btn-success">{{ button_label|default('Valider') }}</button>
    
{{ form_end(form) }}
