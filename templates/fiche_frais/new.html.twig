{% extends 'base.html.twig' %}

{% block title %}Ajouter une nouvelle fiche de frais{% endblock %}

{% block body %}
    <div class="showback">
        <h1 class="bleu-gsb">Ajouter une nouvelle fiche de frais</h1>
    </div>

    <div class="text-center">
        {{ include('fiche_frais/_form.html.twig', {'button_label': 'Valider l\'ajout'}) }}
        <br>
        <a href="{{ path('fiche_frais_index') }}"><button class="btn btn-primary">Retour</button></a>
    </div>
{% endblock %}
