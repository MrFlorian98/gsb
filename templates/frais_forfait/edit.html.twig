{% extends 'base.html.twig' %}

{% block title %}Modification de {{ frais_forfait.libelle }}{% endblock %}

{% block body %}
    <div class="showback">
        <h1 class="bleu-gsb">Modification de {{ frais_forfait.libelle }}</h1>
    </div>

    <div class="text-center">
        {{ include('frais_forfait/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <br>
        <a href="{{ path('frais_forfait_index') }}"><button class="btn btn-primary">Retour</button></a>
    </div>
{% endblock %}
