{{ form_start(form) }}

    <div class="row">

        <div class="col-lg-4 col-md-4 col-sm-12"></div>
        <div class="col-lg-4 col-md-4 col-sm-12">
            <div class="showback">
                <h4 class="bleu-gsb">Informations à renseigner</h4>
                {{ form_row(form.date, {'label': 'Date'}) }}
                {{ form_row(form.libelle, {'label': 'Libellé'}) }}
                {{ form_row(form.montant, {'label': 'Montant (en €)'}) }}
                {{ form_row(form.fkVisiteur, {'label': 'Visiteur'}) }}
            </div>
        </div>
        <div class="col-lg-4 col-md-4 col-sm-12"></div>

    </div>

    {{ form_widget(form) }}
    
    <button class="btn btn-success">{{ button_label|default('Valider') }}</button>
    
{{ form_end(form) }}
