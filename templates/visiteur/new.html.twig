{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouveau visiteur{% endblock %}

{% block body %}
    <div class="showback">
        <h1 class="bleu-gsb">Ajouter un nouveau visiteur</h1>
    </div>

    <div class="text-center">
        {{ include('visiteur/_form.html.twig', {'button_label': 'Valider l\'ajout'}) }}
        <br>
        <a href="{{ path('visiteur_index') }}"><button class="btn btn-primary">Retour</button></a>
    </div>
{% endblock %}
